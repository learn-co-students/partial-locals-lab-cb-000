Classroom Info
<%= render partial: "classroom", locals: {classroom: @classroom} %>
We now want to call out the oldest student in the class:
<br>
Oldest student:
<br>
<%= render partial: "students/student", locals: {student: @classroom.students.sort_by {|student| student.birthday }.first } %>
<br>
All classroom students list
<% @classroom.students.each do |student| %>
  <%= render partial: "students/student", locals: {student: student}  %>
<% end %>
